if Legacy.Settings.Basher.useRage ~= true then
  Legacy.Settings.Basher.useRage = true
  Legacy.echo("<green>Will now use rage while bashing!")
else
  Legacy.Settings.Basher.useRage = false
  Legacy.echo("<red>Will no longer use rage while bashing!")

end